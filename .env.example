# Copy this file to .env and fill in real values. docker-compose reads .env
# automatically from the same directory as docker-compose.yml.

# Required - Gemini API key for RAG/requirement-extraction/chat features.
GEMINI_API_KEY=

# Optional - pin a specific Gemini model instead of auto-discovery.
# GEMINI_MODEL=
